apiVersion: v1
kind: Service
metadata:
  name: {{ include "gocuts.fullname" . }}
  labels:
    {{- include "gocuts.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "gocuts.selectorLabels" . | nindent 4 }}