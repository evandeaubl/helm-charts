apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "gocuts-chart.fullname" . }}-config
  labels:
    {{- include "gocuts-chart.labels" . | nindent 4 }}
data:
  gocuts.toml: |
    {{- .Values.config.toml | nindent 4 }}